name: CI

on:
  pull_request:
  push:
    branches: [main, session-release/shopify-live-sandbox]

permissions:
  contents: read

jobs:
  public-demo-and-bff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: storefront-bff/package-lock.json
      - name: Install locked BFF dependencies
        working-directory: storefront-bff
        run: npm ci
      - name: Audit locked BFF dependencies
        working-directory: storefront-bff
        run: node ../scripts/npm-audit-bounded.mjs
      - name: Test zero-account demo, hosted candidate, and BFF
        run: node --test demo/tests/*.test.mjs hosted-demo/test/*.test.mjs storefront-bff/test/*.test.mjs
      - name: Bundle hosted synthetic demo without deploying
        run: npm run hosted:dry-run
      - name: Verify release metadata alignment
        run: |
          npm run test:release-tools
          npm run verify:release-metadata
      - name: Validate paired synthetic harness contract
        run: npm run test:paired-e2e
      - name: Validate three-repository contract-projection harness
        run: npm run test:triad-contract

  portable-local-contracts:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
      - name: Verify portable artifact, launcher, and browser-path contracts
        run: node --test evals/triad-v1/test/triad-contract.test.mjs demo/tests/platform.test.mjs scripts/tests/browser-path.test.mjs

  shopify-read-only-browser-qa:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.59.1-noble
    env:
      HOME: /root
      PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install locked root dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Install locked source-only account dependencies
        run: npm ci --prefix shopify-customer-account --ignore-scripts --no-audit --no-fund
      - name: Verify complete account preview bundle and actual text sinks
        run: npm run verify:account-preview
      - name: Verify Shopify read-only sandbox in three browsers
        run: |
          mkdir -p build/ci-evidence-input
          node demo/tests/run-shopify-browser-qa.mjs --all > build/ci-evidence-input/shopify-browser.json
      - name: Verify actual Liquid and App Proxy in bundled browser engines
        run: node shopify-theme/tests/run-shopify-liquid-browser-qa.mjs --all
      - name: Generate exact-SHA browser evidence
        run: >-
          node scripts/generate-ci-evidence.mjs
          --kind browser
          --shopify-result build/ci-evidence-input/shopify-browser.json
          --liquid-report work/shopify-liquid-qa/report.json
          --output artifacts/ci-evidence/browser.json
      - name: Upload exact-SHA browser evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reference-store-browser-evidence
          path: artifacts/ci-evidence/browser.json
          if-no-files-found: error
      - name: Upload source-only account preview evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: account-preview-verification
          path: artifacts/account-preview/

  search-contract-v2:
    strategy:
      fail-fast: false
      matrix:
        node-version: ["22", "24"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node-version }}
      - name: Verify browser-to-BFF Search Contract v2 compatibility
        run: node --test storefront-bff/test/worker.test.mjs

  customer-account:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: shopify-customer-account/package-lock.json
      - name: Install locked dependencies
        working-directory: shopify-customer-account
        run: npm ci
      - name: Audit locked dependencies
        working-directory: shopify-customer-account
        run: node ../scripts/npm-audit-bounded.mjs
      - name: Create non-deployable test config
        working-directory: shopify-customer-account
        run: cp shopify.app.toml.example shopify.app.toml
      - name: Test and build
        working-directory: shopify-customer-account
        run: |
          mkdir -p ../artifacts
          set -o > ../artifacts/customer-account-pipeline-shell.txt
          set +e
          (printf '%s\n' 'synthetic-producer-exit-7'; exit 7) | tee ../artifacts/customer-account-pipeline-failure.log
          failure_status=$?
          (printf '%s\n' 'synthetic-producer-exit-0'; exit 0) | tee ../artifacts/customer-account-pipeline-success.log
          success_status=$?
          set -e
          printf 'failure=%s\nsuccess=%s\n' "$failure_status" "$success_status" > ../artifacts/customer-account-pipeline-status.txt
          grep -Fx 'synthetic-producer-exit-7' ../artifacts/customer-account-pipeline-failure.log
          grep -Fx 'synthetic-producer-exit-0' ../artifacts/customer-account-pipeline-success.log
          test "$success_status" -eq 0
          test "$failure_status" -eq 7
          npm test 2>&1 | tee ../artifacts/customer-account-tests.log
          npm run check -- --no-color 2>&1 | tee ../artifacts/customer-account-build.log
          CHROME_PATH=/usr/bin/google-chrome node tests/run-workspace-browser-qa.mjs 2>&1 | tee ../artifacts/customer-account-browser-qa.log
      - name: Upload account evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: customer-account-verification
          path: artifacts/

  theme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
      - name: Install Shopify CLI
        run: npm install --global @shopify/cli@4.6.0
      - name: Install locked parser and test dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Audit locked test dependencies
        run: node scripts/npm-audit-bounded.mjs
      - name: Theme check
        run: shopify theme check --path shopify-theme --fail-level error
      - name: Storefront contract tests
        run: node --test shopify-theme/tests/*.test.mjs
      - name: Browser contract QA
        run: node shopify-theme/tests/run-agent-drawer-browser-qa.mjs

  safety:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
      - name: Scan repository
        run: |
          node scripts/check-doc-links.mjs
          node scripts/scan-public.mjs .
      - name: Generate CycloneDX SBOM
        run: npm run sbom
      - name: Generate exact-SHA safety evidence
        run: >-
          node scripts/generate-ci-evidence.mjs
          --kind safety
          --output artifacts/ci-evidence/safety.json
      - name: Upload exact-SHA safety evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reference-store-safety-evidence
          path: artifacts/ci-evidence/safety.json
          if-no-files-found: error
      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reference-store-sbom
          path: artifacts/sbom/

  dependency-review:
    # GitHub's dependency-review API requires Advanced Security for private
    # repositories. Dependabot alerts and npm audit cover the private RC; this
    # job activates automatically when the repository is made public.
    if: github.event_name == 'pull_request' && github.event.repository.private == false
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: high
