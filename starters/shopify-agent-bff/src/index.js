// Keep the reviewed BFF implementation as the single source of policy truth.
// When this starter is extracted, copy storefront-bff/src/index.js beside this
// file and update the import. Do not replace it with a browser-side token.
import worker from "../../../storefront-bff/src/index.js";

export default worker;
